<%= form_with(model: complain_list, local: true) do |form| %>
  <% if complain_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(complain_list.errors.count, "error") %> prohibited this complain_list from being saved:</h2>

      <ul>
      <% complain_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :complain_status %>
    <%= form.text_field :complain_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
